# Base URL of the Django API. Not yet consumed anywhere — lib/api/* still
# reads from lib/mock/* (see CLAUDE.md lib/api/client.ts note). Set this once
# lib/api/client.ts is wired to the real backend.
NEXT_PUBLIC_API_BASE_URL=http://localhost:8000
